# Phase 6 — Documentation & professionalism polish

**Objective:** Bring the surrounding project docs up to the same bar as the code.

Task hints:
- `CONTRIBUTING.md`: how to set up a dev environment, run tests, submit a PR.
- `CHANGELOG.md`: even a minimal one signals active, tracked development.
- Clean up the ASCII architecture diagram now that Phase 2 has split core/experimental — the diagram should reflect what's actually load-bearing.
- Make sure `LICENSE` file matches the MIT claim in the README (verify it's actually present and correctly filled in, not a placeholder).

**Acceptance criteria:**
- A new contributor can go from clone to running tests using only `CONTRIBUTING.md`.

---
